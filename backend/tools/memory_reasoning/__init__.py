"""Memory & Reasoning bucket of the Tool Bus.

Tools that remember and reason: event_log, personal_baseline,
conversation_memory, regimen_store, anomaly_detector, pattern_absence,
risk_classifier, interaction_check, medical_rag, clinical_consult.

The Event Log lookup tool is the most-used tool in the codebase
(Companion uses it for every memory-recall scenario).
"""
